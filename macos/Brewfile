brew "ast-grep" # 功能=提供 AST 结构化代码搜索；最佳实践=replace；修改级别=建议修改；建议=在 ARM Homebrew 重装以替代 Intel 副本；归属=public；验证=command -v ast-grep 且 file 显示 arm64
brew "cmake" # 功能=提供跨项目原生构建生成器；最佳实践=replace；修改级别=建议修改；建议=保留直接安装项并迁移到 ARM Homebrew；归属=public；验证=cmake --version 且命令来源为 opt-homebrew
brew "gh" # 功能=提供 GitHub 命令行客户端；最佳实践=replace；修改级别=建议修改；建议=在 ARM Homebrew 重装并沿用凭证助手；归属=public；验证=gh auth status 且命令为 arm64
brew "git-lfs" # 功能=提供 Git 大文件扩展；最佳实践=replace；修改级别=建议修改；建议=迁移到 ARM Homebrew 后重新验证 filter；归属=public；验证=git lfs version 且 git lfs env 正常
brew "graphviz" # 功能=提供图形布局与 dot 渲染；最佳实践=replace；修改级别=建议修改；建议=保留直接工具并改由 ARM Homebrew 安装；归属=public；验证=dot -V 且命令为 arm64
brew "mise" # 功能=托管 Bun-Node-pnpm-Gitleaks 精确版本；最佳实践=replace；修改级别=一定要改；建议=作为唯一跨项目运行时版本管理器；归属=public；验证=mise doctor 与配置解析通过
brew "poppler" # 功能=提供 PDF 检查与渲染 CLI；最佳实践=replace；修改级别=建议修改；建议=迁移当前直接工具到 ARM Homebrew；归属=public；验证=pdftoppm -v 且命令为 arm64
brew "ripgrep" # 功能=提供快速文本搜索；最佳实践=replace；修改级别=建议修改；建议=保留工作流并迁移到 ARM Homebrew；归属=public；验证=rg --version 且命令为 arm64
brew "rsync" # 功能=提供现代 rsync 同步能力；最佳实践=replace；修改级别=建议修改；建议=保留当前直接工具并迁移到 ARM Homebrew；归属=public；验证=rsync --version 且来源为 opt-homebrew
brew "silicon" # 功能=把源代码渲染为图片；最佳实践=replace；修改级别=建议修改；建议=保留当前直接工具并迁移到 ARM Homebrew；归属=public；验证=silicon --version 且命令为 arm64
brew "temporal" # 功能=提供 Temporal 开发 CLI；最佳实践=replace；修改级别=建议修改；建议=只迁移 CLI 不自动启停服务；归属=public；验证=temporal --version 且服务状态未变化
brew "tmux" # 功能=提供终端复用；最佳实践=replace；修改级别=建议修改；建议=保留当前直接工具并迁移到 ARM Homebrew；归属=public；验证=tmux -V 且命令为 arm64
brew "tree" # 功能=提供目录树展示；最佳实践=replace；修改级别=建议修改；建议=保留当前直接工具并迁移到 ARM Homebrew；归属=public；验证=tree --version 且命令为 arm64
brew "uv" # 功能=统一 Python 版本项目环境和 CLI tool；最佳实践=replace；修改级别=一定要改；建议=由 ARM Homebrew 提供宿主并只管理 uv Python；归属=public；验证=uv --version 且 file 显示 arm64
brew "vhs" # 功能=提供终端演示录制；最佳实践=replace；修改级别=建议修改；建议=保留当前直接工具并迁移到 ARM Homebrew；归属=public；验证=vhs --version 且命令为 arm64
brew "zoxide" # 功能=提供智能目录跳转；最佳实践=replace；修改级别=一定要改；建议=导入 autojump 数据后替代 Intel autojump；归属=public；验证=zoxide --version 且常用目录可跳转
